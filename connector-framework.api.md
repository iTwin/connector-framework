## API Report File for "@itwin/connector-framework

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AccessToken } from '@bentley/itwin-client';
import { AuthorizedClientRequestContext } from '@bentley/itwin-client';
import { BentleyStatus } from '@bentley/bentleyjs-core';
import { ClientRequestContext } from '@bentley/bentleyjs-core';
import { Element } from '@bentley/imodeljs-backend';
import { Id64String } from '@bentley/bentleyjs-core';
import { IModelDb } from '@bentley/imodeljs-backend';
import { IModelStatus } from '@bentley/bentleyjs-core';
import { Subject } from '@bentley/imodeljs-backend';

// @beta
export class ConnectorJobDefArgs {
    allDocsProcessed: boolean;
    argsJson: any;
    connectorModule?: string;
    dmsAccessToken?: string;
    dmsServerUrl?: string;
    // (undocumented)
    documentGuid?: string;
    doDetectDeletedElements: boolean;
    isSnapshot: boolean;
    outputDir?: string;
    revisionComments?: string;
    sourcePath?: string;
    updateDbProfile: boolean;
    updateDomainSchemas: boolean;
}

// @public
export enum ConnectorLoggerCategory {
    Framework = "itwin-connector.Framework"
}

// @beta
export class ConnectorRunner {
    constructor(jobArgs: ConnectorJobDefArgs, serverArgs?: ServerArgs | IModelBankArgs);
    static fromArgs(args: string[]): ConnectorRunner;
    // (undocumented)
    getCacheDirectory(): string | undefined;
    synchronize(): Promise<BentleyStatus>;
}

// @beta
export abstract class Connector {
    // (undocumented)
    abstract getApplicationId(): string;
    // (undocumented)
    abstract getApplicationVersion(): string;
    // (undocumented)
    abstract getConnectorName(): string;
    getDataChangesDescription?(): string;
    getJobSubjectName(sourcePath: string): string;
    abstract importDefinitions(): Promise<any>;
    abstract importDomainSchema(requestContext?: AuthorizedClientRequestContext | ClientRequestContext): Promise<any>;
    abstract importDynamicSchema(requestContext?: AuthorizedClientRequestContext | ClientRequestContext): Promise<any>;
    abstract initialize(params: ConnectorJobDefArgs): any;
    abstract initializeJob(): Promise<void>;
    set jobSubject(subject: Subject);
    // (undocumented)
    get jobSubject(): Subject;
    onOpenIModel(): Promise<BentleyStatus>;
    abstract openSourceData(sourcePath: string): Promise<void>;
    supportsMultipleFilesPerChannel(): boolean;
    set synchronizer(sync: Synchronizer);
    // (undocumented)
    get synchronizer(): Synchronizer;
    abstract updateExistingData(): Promise<any>;
}

// @beta (undocumented)
export const loggerCategory: string;


// (No @packageDocumentation comment for this package)

```
